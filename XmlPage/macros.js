function href_macro (param)
 {
  return (this.href());
 }

function id_macro (param)
 {
  return ("" + this._id);
 }

function xmltext_macro (param)
 {
  return ("" + this.xmltext);
 }

function username_macro (param)
 {
  return ("" + this.username);
 }

function action_macro (param)
 {
  return ("" + this.href(param));
 }
